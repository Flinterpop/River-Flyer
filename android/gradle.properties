org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=false
android.nonTransitiveRClass=true
# AGP 9 enables built-in Kotlin by default, which adds the Kotlin standard
# library to the APK (a 2 MB classes.dex); the app has no Java or Kotlin.
android.builtInKotlin=false
